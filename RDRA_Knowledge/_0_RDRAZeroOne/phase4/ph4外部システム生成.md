# phase4：外部システム
- 「0_RDRAZeroOne/phase3/ph3BUC.tsv」を読み外部システムの役割を理解する
- 以後の「ph3BUC.tsv」の記述は上記のファイルのパスを指す
- 上記ファイルから外部システムを再整理し「ph4外部システム.tsv」を出力する

# 背景理解
- 「RDRA_Knowledge/_1_RDRA/RDRA.md」を読込み理解してください
- 「外部システム」は業務を実現するために連携する外部のシステムを表す
  
## 外部システムの洗出し
- 「ph3BUC.tsv」から外部システムを抽出する
- 外部システムは外部システム群としてグループ化する
- 外部システム群は役割で分類する

## 出力ルール
- 出力内容について判断に迷った場合は可能性の一番高いものを選択する
- 出力には必ずヘッダー（カラム名）行を出力する
- 空フィールドにするときは、タブ区切りを省略しない
- プログラムは絶対に生成しない。

### 「外部システム」出力フォーマット
出力先：0_RDRAZeroOne/phase4/ph4外部システム.tsv
出力形式：TSV（タブ区切り）
カラム構成：
1列目: 外部システム群：外部システムの役割に応じて分類する
2列目: 外部システム：システムに関わる外部のシステム。外部システム名は役割が分かる名前にする
3列目: 役割：外部システムの役割の内容説明
- ヘッダー行を含め、各フィールドはタブ文字で区切ってください。

## 出力チェックリスト
- [ ] 各行が3カラム（2個のタブ区切り）であることを確認する

## 今すぐ実行
上記の指示に従い、今すぐ実行してください。
質問や確認は不要です。
ファイルを出力終了後は要約やまとめの出力は行わず直ちに終了してください


